# Python build artefacts
__pycache__/
*.py[cod]
*.egg-info/
*.egg
dist/
build/

# uv
.venv/
.uv/

# Install script backups
*.bak

# Local planning (not versioned)
TODO.md

# Secrets / local env (never commit)
.env
.env.*
*.env
!.env.example

# Generated / local state
.cursor/
.deepeval/
graphify-out/
.pytest_cache/

# Conversation / transcript data — NEVER commit
# Eval fixtures contain ground-truth derived from real conversations
tests/eval/fixtures/
tests/eval/.env
# Raw Cursor transcripts and Curios export/import archives
*.jsonl
transcripts/
curios-export*.tar.gz
curios-export*.tgz
manifest.json
# Local Chroma DB in case CURIOS_DATA ever points at the repo
chromadb/
*.sqlite3
*.sqlite3-journal

# Tool config files that are local-only
.graphifyignore

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~
